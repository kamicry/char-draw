name: charpic # 插件唯一识别名，以 astrbot_plugin_ 前缀开头
display_name: 字符画生成器 # 用于展示的名字，可以是方便人类阅读的名字（需要版本 >= v4.5.0，低版本不会报错，请放心填写）
desc: 将图片转换为ASCII艺术字符画的插件，支持静态图片和GIF。 # 插件简短描述
version: v1.0.0 # 插件版本号。格式：v1.1.1 或者 v1.1
author:移植自1umine的nonebot_plugin_charpic # 作者
repo: https://github.com/1umine/nonebot_plugin_charpic # 插件的仓库地址
dependencies:
  - Pillow>=9.0.0
  - httpx>=0.27.0
  - imageio>=2.9.0,<3.0
